# AI Prompt Engineering Log

**Project:** DE-05 — ETL Script Documentation Generator (LENS)
**Team:** ETLens AI

> **Note:** GitHub Copilot was used as AI coding assistant throughout all development steps as per the mandatory AI-Assisted Development requirement.

---

| # | Agent / Component | AI Tool | Prompt Summary | Purpose | Output |
|---|---|---|---|---|---|
| 1 | Market Gap Analysis | Claude AI | Analyze existing solutions for undocumented ETL scripts. For each solution explain what it does and its key limitations. Give a comparison table showing what companies need vs what exists today highlighting gaps for small and medium companies using plain Python and SQL files. | Identify market gaps and justify the need for DE-05 | Comparison table showing 5 key gaps: no tool for plain Python/SQL files, no free solution, no automatic processing, no readable docs, no free diagram generation. |
| 2 | Tech Stack Selection & Gap Mapping | Claude AI | You are a senior software architect. I am building an ETL Script Documentation Generator. Recommend a complete tech stack and explain what each technology does in this project. Map each technology to the specific gap it fixes in existing solutions. | Choose the right tools and justify each choice against the 5 identified market gaps | Final tech stack: Python, Streamlit, FastAPI, Ollama+Llama3, Sentence Transformers, FAISS, LangChain, AST, sqlparse, Graphviz, NetworkX, SQLite, fpdf2, GitHub Copilot. |
| 3 | Python ETL Parser (AST) | Claude AI + GitHub Copilot | Write a Python function using the AST module that reads a Python ETL file and extracts: data sources (read_csv, read_sql), transformation operations (merge, filter, rename, groupby), and target destinations (to_sql, to_csv). Return results as a structured dictionary. | Build the core Python ETL file parser that reads and understands Python scripts without needing comments | python_parser.py — extracts sources, targets, and transformations from any Python ETL file using AST walking. |
| 4 | SQL ETL Parser (sqlparse) | Claude AI + GitHub Copilot | Write a Python function using sqlparse that reads a SQL ETL file and extracts: INSERT INTO targets, FROM and JOIN sources, and transformation keywords like WHERE, GROUP BY, SUM, COUNT. Handle whitespace tokens carefully and return a structured dictionary. | Build the SQL ETL file parser that understands SQL scripts and extracts data lineage | sql_parser.py — correctly extracts sources, targets, and transformations from SQL files. |
| 5 | MCP Tool — AST Reader Tool | Claude AI + GitHub Copilot | Create a LangChain BaseTool called ASTReaderTool that wraps the Python and SQL parsers. The tool should accept a file path, detect the file extension, call the appropriate parser, and return the result as JSON. Include proper error handling for missing files and unsupported formats. | Satisfy the mandatory MCP Tool requirement — wraps parsers as a callable tool that the LangChain agent uses inside its agent loop | ast_reader_tool.py — ASTReaderTool class that the agent calls to read ETL files, satisfying mandatory MCP capability. |
| 6 | Ollama LLM Integration | Claude AI + GitHub Copilot | Write a Python class OllamaClient that connects to a locally running Ollama server at http://127.0.0.1:11434. It should send a prompt to the llama3 model via REST API using requests, handle timeouts and errors gracefully, and return the model response as a string. | Connect the project to the free local LLM — enables all AI-powered explanation and documentation generation | llm_client.py — OllamaClient class that sends prompts to Llama3 and returns plain English explanations. |
| 7 | Documentation Generator | Claude AI + GitHub Copilot | Write a Python function that takes the parsed ETL metadata dictionary (sources, targets, transformations) and sends it to the Ollama LLM with this prompt: 'You are a technical documentation writer. Given this ETL script metadata, write clear plain English documentation explaining what this script does, what data it reads, what transformations it applies, and where the output goes.' | Fix gap 1 — Automatic Documentation Generation. Converts raw parser output into human readable Markdown documentation | doc_generator.py — generates plain English Markdown documentation for every ETL script automatically. |
| 8 | Business Purpose Explainer | Claude AI + GitHub Copilot | Write a Python function that sends ETL metadata to Ollama with this prompt: 'You are a business analyst. Given this ETL script that reads from these sources and writes to these targets with these transformations — explain in 2-3 sentences WHY this ETL script exists. What business problem does it solve? What decision or report does it enable?' | Fix gap 2 — Business Purpose Explanation. Explains why an ETL script exists, not just what it does. No existing tool does this. | business_explainer.py — generates business context explanation for every ETL script using LLM reasoning. |
| 9 | Graphviz Flow Diagram Generator | Claude AI + GitHub Copilot | Write a Python function using the graphviz library that takes ETL metadata (sources, transformations, targets) and generates a directed graph diagram. Sources should be green boxes on the left, transformation steps should be blue boxes in the middle, and targets should be orange boxes on the right. Save as PNG to output/diagrams/. | Fix gap 3 — Automatic Flow Diagram Generation. Draws the data flow automatically so anyone can visualize the ETL pipeline | flow_diagram.py — generates source to transformation to target PNG diagrams for every ETL script automatically. |
| 10 | NetworkX Impact Analysis | Claude AI + GitHub Copilot | Write a Python function using NetworkX that processes all ETL scripts in a folder, builds a directed dependency graph where nodes are tables/files and edges represent data flow, and identifies what downstream scripts or tables are affected if a given script changes or fails. Return the impact list with severity levels. | Fix gap 4 — Impact Analysis. Shows what breaks if one ETL script changes. No free tool does this. | impact_analysis.py — builds dependency graph and returns downstream impact list for any ETL script. |
| 11 | FAISS RAG Pipeline | Claude AI + GitHub Copilot | Write a Python class using LangChain, Sentence Transformers, and FAISS that: embeds all generated ETL documentation into a FAISS vector store, retrieves the most relevant past documentation when processing a new ETL file, and provides that context to the LLM for more consistent and accurate documentation generation. | Satisfies mandatory agent loop requirement and improves LLM accuracy by providing relevant context from previously processed ETL scripts | rag_pipeline.py — FAISS vector store with Sentence Transformer embeddings for context-aware documentation generation. |
| 12 | FastAPI Backend | Claude AI + GitHub Copilot | Write a FastAPI application with these endpoints: POST /process — accepts a folder path, runs the agent loop over all ETL files, returns documentation results. GET /results — returns all processed results from SQLite. GET /impact/{filename} — returns impact analysis for a specific file. Include CORS middleware for Streamlit frontend connection. | Build the professional REST API backend that connects all modules and serves results to the Streamlit frontend | backend/main.py — FastAPI app with 3 endpoints handling ETL processing, results retrieval, and impact analysis. |
| 13 | Streamlit Frontend | Claude AI + GitHub Copilot | Build a Streamlit web application with: a folder upload/path input section, a process button that calls the FastAPI backend, a results panel showing documentation, business purpose, flow diagram image, and impact analysis for each processed ETL file, and a PDF export button. Use a clean professional layout with sidebar navigation. | Build the user-facing web UI where managers and developers interact with the tool — makes the solution accessible to non-technical users | frontend/app.py — complete Streamlit UI with file processing, results display, diagram rendering, and PDF export. |
| 14 | PDF Export Functionality | Claude AI + GitHub Copilot | Write a Python function using fpdf2 that takes the generated documentation dictionary for an ETL script and exports it as a formatted PDF report containing: title with filename, plain English documentation section, business purpose section, impact analysis section, and a placeholder reference to the flow diagram. Save to output/reports/. | Enable downloadable PDF reports so documentation can be shared with teams and stored for compliance | pdf_exporter.py — generates formatted PDF reports for every processed ETL script with all four sections. |
| 15 | README and GitHub Setup | Claude AI | Write a professional GitHub README.md for a hackathon project called LENS — ETL Script Documentation Generator. Include: project overview, the 4 gaps it fixes, tech stack table, installation steps, how to run instructions, team name, and a section on mandatory AI capabilities demonstrating agent loop, MCP tool, and external API integration. | Complete the mandatory source control requirement and document the project professionally for judges and GitHub reviewers | README.md — complete project documentation covering setup, usage, architecture, and mandatory requirement demonstration. |

---

## Mandatory Requirements Checklist

| Requirement | How Satisfied |
|---|---|
| AI Assisted Development | GitHub Copilot used while writing every file. Claude AI used for architecture and code guidance throughout. |
| Prompt Documentation | This file — 15 prompts documented covering every component of the project. |
| Agent Loop | LangChain agent loop automatically processes every ETL file in the folder one by one without human involvement. |
| MCP Tool (Built) | ASTReaderTool — custom built LangChain BaseTool that the agent calls to read and parse ETL files. |
| External API / Service Integration | Ollama REST API called via requests at http://127.0.0.1:11434 for all LLM explanation and documentation generation. |
